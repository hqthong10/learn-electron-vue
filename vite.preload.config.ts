// Cấu hình cho preload scripts (cầu nối giữa main và renderer)
// Môi trường: Node.js context nhưng chạy trong renderer
// Chức năng: Expose API an toàn từ main process đến renderer
import { defineConfig } from 'vite';

// https://vitejs.dev/config
export default defineConfig({});
